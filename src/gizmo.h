#pragma once
#include "app_state.h"

// Draw the camera orientation gizmo (top-right corner).
// Shows X/Y/Z axes matching the current view, with clickable labels
// to snap to standard views (Front, Back, Top, Bottom, Left, Right).
void drawViewGizmo();
